from DiGraph import DiGraph

"""
 * regular bfs using tag of each node to mark whether the node as been visited or not
     *
     * 0 -> not_visited
     * 1 -> visited
     *
     *
"""


def bfs(graph: DiGraph) -> bool:

    return True
